# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Binaries
client/cmd/gophepass/main
client/cmd/gophepass/gophepass
cclient/cmd/staticlint/staticlint*

# Dependency directories (remove the comment below to include it)
vendor/

# IDEs directories
.idea
.vscode

# Config files
client/cmd/gophepass/gophepass.db
client/cmd/gophepass/gophepass.json
*.log
